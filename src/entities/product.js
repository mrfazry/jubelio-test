class Product {
  constructor(name, sku, imageURL, description, price) {
    this.name = name;
    this.sku = sku;
    this.imageURL = imageURL;
    this.description = description;
    this.price = price;
  }
}

module.exports = Product;
